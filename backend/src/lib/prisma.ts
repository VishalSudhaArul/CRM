// DEPRECATED: Prisma ORM has been replaced with raw SQL database engine (PostgreSQL / SQLite).
// Use import { query, queryOne, executeTransaction } from "./db"; instead.
export { query as rawQuery } from "./db";